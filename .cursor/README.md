# Cursor configuration

Migrated from `.agents/` and `.github/` (instructions, agents, prompts).

- **rules/** — `.mdc` files with YAML frontmatter (`globs`, `alwaysApply`)
- **skills/** — one folder per skill with `SKILL.md` (and optional `references/`, `scripts/`, `data/`)

See [AGENTS.md](../AGENTS.md) for the full index.
